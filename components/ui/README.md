# components/ui

Reusable low-level UI primitives and controls live here.

- Put shared controls such as buttons, sliders, toggles, tabs, tooltips, and reset/play controls here.
- Keep components accessible and small enough to reuse across visualizers.
- Avoid course-specific business logic in UI primitives.
