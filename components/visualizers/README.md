# components/visualizers

Interactive tool-level visualizer components live here.

- Use focused `"use client"` boundaries only for components that need state, hooks, events, browser APIs, or D3 gestures.
- Keep algorithm calculations delegated to `lib/algorithms`.
- Keep shared plot building blocks delegated to `components/plots`.
