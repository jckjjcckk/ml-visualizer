# lib/algorithms

Pure deterministic machine learning logic lives here.

- Keep algorithms independent from React components, browser APIs, and UI state.
- Prefer typed inputs and outputs that are easy to unit test.
- Keep visualization-specific formatting in components, not algorithm modules.
